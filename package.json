{
  "name": "websocket",
  "version": "1.0.0",
  "description": "training to websocket with React & Node.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "concurrently \"npm install\" \"npm install --prefix backend\" \"npm install --prefix frontend\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yiroma/websocket.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Yiroma/websocket/issues"
  },
  "homepage": "https://github.com/Yiroma/websocket#readme",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
